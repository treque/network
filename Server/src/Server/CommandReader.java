package Server;
import java.util.*;

public class CommandReader {
	enum Command{
		LS,
		CD,
		MKDIR,
		UPLOAD,
		DOWNLOAD,
		EXIT
	};
	
	public void executor(String input) {
		switch(input) {
		case("ls"):
			;
		}
		
	};
}
